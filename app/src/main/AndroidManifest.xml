<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ternavest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ternavest">
        <activity android:name=".ui.peternak.kelola.EditProyekActivity"></activity>
        <activity android:name=".ui.peternak.kelola.LaporanActivity" />
        <activity android:name=".ui.SettingsActivity" />
        <activity android:name=".ui.DetailProfileActivity" />
        <activity android:name=".ui.peternak.PeternakActivity" />
        <activity android:name=".ui.invest.InvestorActivity" />
        <activity android:name=".testing.WilayahTest" />
        <activity android:name=".ui.peternak.kelola.TambahProyekActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>